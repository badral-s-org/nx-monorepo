{
  "name": "@org/secret-manager-web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "jest",
    "deploy": "vercel pull --yes --environment=production && vercel build --prod && vercel deploy --prebuilt --prod",
    "vercel-build": "npx nx build secret-manager-web"
  },
  "dependencies": {
    "next": "~16.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
