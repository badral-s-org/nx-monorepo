name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  actions: read
  contents: read

jobs:
  ci-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          filter: tree:0
          fetch-depth: 0

      - name: Derive appropriate SHAs for Nx affected commands
        uses: nrwl/nx-set-shas@v4

      - run: npx nx start-ci-run --distribute-on="3 linux-medium-js" --stop-agents-after="build"

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Affected Builds
        run: npx nx affected -t build

      - run: npx nx fix-ci
        if: always()

  # cd-deploy:
  #   needs: ci-build # CI амжилттай болсны дараа ажиллана
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         filter: tree:0
  #         fetch-depth: 0

  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 24
  #         cache: 'npm'

  #     - name: Install Dependencies
  #       run: npm ci

  #     # Main дээр нэгдсэн өөрчлөлтүүдийг deploy хийнэ
  #     # (Жич: Таны аппликейшнуудын project.json дотор "deploy" target суусан байх шаардлагатай)
  #     - name: Deploy Affected Apps
  #       run: npx nx affected -t deploy
  #       env:
  #         # Шаардлагатай бол deploy-д ашиглах нууц үг, хандах эрхүүдийг энд дамжуулна
  #         DEPLOY_SECRET_KEY: ${{ secrets.DEPLOY_SECRET_KEY }}
